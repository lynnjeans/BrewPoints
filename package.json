{
  "name": "brewpoints",
  "private": true,
  "version": "0.0.0",
  "description": "BrewPoints — digital coffee loyalty PWA (monorepo root)",
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm:dev:client\" \"npm:dev:server\"",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "lint": "npm --prefix client run lint && npm --prefix server run lint",
    "typecheck": "npm --prefix client run typecheck && npm --prefix server run typecheck",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "install:all": "npm install && cd client && npm install && cd .. && cd server && npm install && cd .."
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prettier": "^3.4.2"
  }
}
